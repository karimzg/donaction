import { TmplCategoryPipe } from './tmpl-category.pipe';

describe('TmplCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TmplCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
