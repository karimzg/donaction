import { TmplNbByLibraryPipe } from './tmpl-nb-by-library.pipe';

describe('TmplNbByLibraryPipe', () => {
  it('create an instance', () => {
    const pipe = new TmplNbByLibraryPipe();
    expect(pipe).toBeTruthy();
  });
});
