import { TapContentSidebarTitlePipe } from './tap-content-sidebar-title.pipe';

describe('TapContentSidebarTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TapContentSidebarTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
