import { StatusDonPaymentPipe } from './status-don-payment.pipe';

describe('StatusDonPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusDonPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
