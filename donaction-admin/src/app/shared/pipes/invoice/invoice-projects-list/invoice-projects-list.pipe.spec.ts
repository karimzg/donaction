import { InvoiceProjectsListPipe } from './invoice-projects-list.pipe';

describe('InvoiceProjectsListPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceProjectsListPipe();
    expect(pipe).toBeTruthy();
  });
});
