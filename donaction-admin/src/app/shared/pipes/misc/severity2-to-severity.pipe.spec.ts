import { Severity2ToSeverityPipe } from './severity2-to-severity.pipe';

describe('Severity2ToSeverityPipe', () => {
  it('create an instance', () => {
    const pipe = new Severity2ToSeverityPipe();
    expect(pipe).toBeTruthy();
  });
});
