import { ConnectedProfileHasSameRolePipe } from './connected-profile-has-same-role.pipe';

describe('ConnectedProfileHasSameRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectedProfileHasSameRolePipe();
    expect(pipe).toBeTruthy();
  });
});
