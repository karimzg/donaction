import { FormControlCastPipe } from './form-control-cast.pipe';

describe('FormControlCastPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlCastPipe();
    expect(pipe).toBeTruthy();
  });
});
