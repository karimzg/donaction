import { ProfileRoleLabelPipe } from './profile-role-label.pipe';

describe('ProfileRoleLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileRoleLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
