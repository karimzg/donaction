import { ScrollNearEndDirectiveDirective } from './scroll-near-end-directive.directive';

describe('ScrollNearEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNearEndDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
