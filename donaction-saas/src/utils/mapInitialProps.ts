export function MapInitialProps(props) {
  // onMount(() => {
  //   // Checking props validity
  //   numberProp = Number(numberProp);
  //   booleanProp = booleanProp === 'true';
  //   objectProp = typeof objectProp === 'string' ? JSON.parse(objectProp) : objectProp;
  //   arrayProp = typeof arrayProp === 'string' ? JSON.parse(arrayProp) : arrayProp;
  // });
  // <p>Number: {numberProp} : {typeof numberProp}</p>
  // <p>Boolean: {booleanProp} : {typeof booleanProp}</p>
  // <p>Object: {JSON.stringify(objectProp)} : {typeof objectProp}</p>
  // <p>Array: {arrayProp} : {typeof arrayProp}</p>
}
