import { LastProfileUsedPipe } from './last-profile-used.pipe';

describe('LastProfileUsedPipe', () => {
  it('create an instance', () => {
    const pipe = new LastProfileUsedPipe();
    expect(pipe).toBeTruthy();
  });
});
