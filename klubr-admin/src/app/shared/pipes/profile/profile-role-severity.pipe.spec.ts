import { ProfileRoleSeverityPipe } from './profile-role-severity.pipe';

describe('ProfileRoleSeverityPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileRoleSeverityPipe();
    expect(pipe).toBeTruthy();
  });
});
