import { LastUpdatedAtPipe } from './last-updated-at.pipe';

describe('LastUpdatedAtPipe', () => {
  it('create an instance', () => {
    const pipe = new LastUpdatedAtPipe();
    expect(pipe).toBeTruthy();
  });
});
