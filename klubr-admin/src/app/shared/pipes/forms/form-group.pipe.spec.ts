import { FormGroupPipe } from './fomr-group.pipe';

describe('FomrGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
