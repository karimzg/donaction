/**
 * Default routes file — empty to prevent Strapi from auto-generating
 * CRUD routes (POST, PUT, DELETE) that would bypass admin-only middleware.
 * All routes are defined in webhook-log-custom.ts.
 */
export default { routes: [] };
